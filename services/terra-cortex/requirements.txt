# ============================
# ⚙️ Core Web Framework
# ============================
fastapi==0.109.0                 # Main API framework for Terra-Cortex (HTTP ingress)
uvicorn[standard]==0.27.0        # ASGI server; runs FastAPI app with websockets support

# ============================
# 🔄 Messaging & Config
# ============================
aiokafka==0.10.0                 # Async Kafka consumer/producer for streaming sensor events
pydantic==2.7.4                  # Data validation; minimum supported by LangChain 0.3.x
pydantic-settings==2.4.0         # Structured settings; required by langchain-community 0.3.x
PyJWT==2.13.0                    # Short-lived scoped service JWTs; includes 2026 security fixes

# ============================
# 🤖 Cloud LLM Integration
# ============================
openai==1.54.0                   # Unified OpenAI/Ollama client (local + cloud LLMs)
httpx==0.27.2                    # Required by OpenAI client; pinned for compatibility

# ============================
# 🧠 RAG (Retrieval-Augmented Generation)
# ============================
langchain==0.3.30                # RAG orchestration; fixes CVE-2026-45134
langchain-community==0.3.27      # Integrations; fixes known pickle, SSRF, and XML issues
chromadb==0.4.22                 # Local vector DB for document embeddings
pypdf==3.17.4                    # PDF parsing for document ingestion
sentence-transformers==2.2.2     # Embedding model loader (MiniLM, MPNet, etc.)
huggingface-hub==0.25.2          # Pinned for compatibility; avoids cached_download removal
cryptography>=3.4                # Required for AES-encrypted PDF parsing (pypdf decryption)

# ============================
# 📈 Monitoring (optional)
# ============================
prometheus-client==0.19.0        # Expose Prometheus metrics from Terra-Cortex
psutil==5.9.5                    # System-level CPU/Mem metrics for Prometheus monitoring
