# Demo data and local BCrypt credentials are deliberately excluded from the
# default/production migration path. Docker Compose opts into this location.
spring.flyway.locations=classpath:db/migration,classpath:db/local
