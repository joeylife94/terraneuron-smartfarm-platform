services:
  terra-ops:
    environment:
      APP_COMMAND_ACK_TIMEOUT_SECONDS: "${E2E_ACK_TIMEOUT_SECONDS:-3}"
      APP_COMMAND_ACK_TIMEOUT_SCAN_MS: "${E2E_ACK_TIMEOUT_SCAN_MS:-500}"
      APP_COMMAND_OUTBOX_MAX_ATTEMPTS: "${E2E_OUTBOX_MAX_ATTEMPTS:-8}"
      APP_COMMAND_OUTBOX_BASE_BACKOFF_SECONDS: "${E2E_OUTBOX_BASE_BACKOFF_SECONDS:-2}"
